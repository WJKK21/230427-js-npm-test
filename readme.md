
html 파일 생성기 
node.js commander 기능 사용 
기본 명령어 node app.js -r  -t 끝? -f 끝 -p 내용도끝!

1. -r body 태그 안에 존재하는 root 설정 가능 // 기본 값 false로 -r 입력시 root 생김 -r 입력 안하면 안생김 
2. -t로 title 태그 내부 텍스트 설정 가능
3. -f 파일 명 설정 가능 확장자는 모두 .html로 생성됨
4. -p body 태그 안 p태그 내용 설정 가능
